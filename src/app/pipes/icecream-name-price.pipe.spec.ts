import { IcecreamNamePricePipe } from './icecream-name-price.pipe';

describe('IcecreamNamePricePipe', () => {
  it('create an instance', () => {
    const pipe = new IcecreamNamePricePipe();
    expect(pipe).toBeTruthy();
  });
});

